# 全局配置文件
spring:
  application:
    name: hdfs-api
  server:
    port: 8080
  datasource:
    url: jdbc:mysql://172.21.44.162:3306/HDFS?useSSL=false&allowPublicKeyRetrieval=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: tangxw
    password: Puersai.168
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      max-file-size: 10240MB
      max-request-size: 102400MB

  # Session配置
  session:
    store-type: memory
    timeout: 30m  # Session超时时间30分钟
    cookie:
      name: JSESSIONID
      http-only: false  # 允许JavaScript访问Cookie
      secure: false     # 开发环境不使用HTTPS
      same-site: lax    # 跨站请求策略

# Hadoop配置
hadoop:
  hdfs:
    user: Administrator
    uri: hdfs://172.21.44.162:9000
    root-path: /user/

logging:
  level:
    root: INFO
    com.computerscience.hdfsapi: DEBUG


server:
  # 服务器的HTTP端口，默认为8080
  port: 8080
  # 移除 context-path 配置，让前端代理直接转发到根路径
  # servlet:
  #   context-path: /api

